import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    formWrapper: {
        padding: 20,
        // height: 1600
    },
    formFieldWrapper: {
        marginTop: 10,
        marginBottom: 10,
    },
    selectWrapper: {
        // minHeight: 58,
    },
    flag: {
        height: 40,
        width: 40,
    },
});

export default styles